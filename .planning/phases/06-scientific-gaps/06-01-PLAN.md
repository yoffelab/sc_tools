---
phase: 06-scientific-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sc_tools/qc/metadata.py
  - sc_tools/tl/celltype/annotate.py
  - sc_tools/tests/conftest.py
  - sc_tools/tests/test_subject_metadata.py
  - sc_tools/tests/test_panel_dispatch.py
autonomous: true
requirements: [SCI-03, SCI-04]

must_haves:
  truths:
    - "Multi-sample projects with missing subject_id produce a warning message"
    - "subject_id identical to library_id produces a distinctness warning"
    - "Batch-condition confounding is detected and warned"
    - "Panel datasets (n_vars < 1000) restrict cell typing to sctype and custom_gates"
    - "force_method=True overrides panel restriction with a logged warning"
    - "Whole-transcriptome datasets (n_vars >= 1000) are not restricted"
  artifacts:
    - path: "sc_tools/qc/metadata.py"
      provides: "validate_subject_metadata and check_confounding functions"
      exports: ["validate_subject_metadata", "check_confounding"]
    - path: "sc_tools/tl/celltype/annotate.py"
      provides: "Panel guard in annotate_celltypes with PANEL_VALIDATED_METHODS"
      contains: "PANEL_VALIDATED_METHODS"
    - path: "sc_tools/tests/test_subject_metadata.py"
      provides: "Unit tests for subject_id validation and confounding"
    - path: "sc_tools/tests/test_panel_dispatch.py"
      provides: "Unit tests for panel detection and method restriction"
  key_links:
    - from: "sc_tools/qc/metadata.py"
      to: "sc_tools/qc/__init__.py"
      via: "module export"
      pattern: "validate_subject_metadata"
    - from: "sc_tools/tl/celltype/annotate.py"
      to: "sc_tools/tl/celltype/_base.py"
      via: "get_backend call after panel guard"
      pattern: "PANEL_VALIDATED_METHODS"
---

<objective>
Add subject-level metadata validation (SCI-03) and panel-aware cell typing dispatch (SCI-04).

Purpose: Subject_id enforcement prevents pseudoreplication in downstream DE analysis. Panel detection prevents applying whole-transcriptome models to targeted panels where they produce unreliable results.

Output: `sc_tools/qc/metadata.py` with validation functions, modified `annotate_celltypes()` with panel guard, and comprehensive tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scientific-gaps/06-CONTEXT.md
@.planning/phases/06-scientific-gaps/06-RESEARCH.md

@sc_tools/qc/sample_qc.py
@sc_tools/tl/celltype/annotate.py
@sc_tools/tl/celltype/_base.py
@sc_tools/tests/conftest.py
@sc_tools/errors.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From sc_tools/tl/celltype/annotate.py:
```python
def annotate_celltypes(
    adata: ad.AnnData,
    method: str,
    *,
    cluster_key: str = "leiden",
    result_key: str | None = None,
    store_proba: bool = False,
    copy: bool = False,
    **kwargs,
) -> ad.AnnData:
```

From sc_tools/tl/celltype/_base.py:
```python
def get_backend(method: str) -> CelltypeBackend
def _store_results(adata, key, labels, scores, proba, metadata, store_proba)
```

From sc_tools/errors.py:
```python
class SCToolsDataError(SCToolsError):  # exit code 2
class SCToolsUserError(SCToolsError):  # exit code 1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subject metadata validation module + tests (SCI-03)</name>
  <files>sc_tools/qc/metadata.py, sc_tools/tests/conftest.py, sc_tools/tests/test_subject_metadata.py, sc_tools/qc/__init__.py</files>
  <read_first>
    - sc_tools/qc/sample_qc.py
    - sc_tools/qc/__init__.py
    - sc_tools/tests/conftest.py
    - sc_tools/errors.py
    - .planning/phases/06-scientific-gaps/06-RESEARCH.md (Pattern 3: Subject ID Validation)
  </read_first>
  <behavior>
    - Test: multi-sample adata missing subject_id returns warning containing "subject_id"
    - Test: multi-sample adata with subject_id == library_id (1:1 mapping) returns distinctness warning
    - Test: single-sample adata missing subject_id returns informational warning (not error)
    - Test: valid multi-sample adata with distinct subject_id returns empty warnings list
    - Test: perfect batch-condition confounding (each batch maps to exactly one condition) detected
    - Test: non-confounded data returns no confounding warning
    - Test: auto-detect multi_sample from multiple unique library_id values
  </behavior>
  <action>
    1. Add shared fixtures to `sc_tools/tests/conftest.py`:
       - `adata_multi_subject`: 6 subjects (3 control, 3 treatment), 50 cells/subject, 200 genes, 3 celltypes. Uses `rng.negative_binomial(5, 0.3, ...)` for raw counts. obs columns: subject_id, library_id (1 lib per subject), condition, batch, celltype. Batch 0 = first 3 subjects, batch 1 = last 3 subjects. NOT confounded (each batch has both conditions).
       - `adata_panel`: same structure but n_vars=40 (IMC-like targeted panel).

    2. Create `sc_tools/tests/test_subject_metadata.py` with tests per behavior list above. Import from `sc_tools.qc.metadata`.

    3. Create `sc_tools/qc/metadata.py` implementing (per D-08, D-09, D-10):
       - `validate_subject_metadata(adata, *, multi_sample=None, subject_key="subject_id", library_key="library_id", condition_key=None, batch_key=None) -> list[str]`
         - Auto-detect multi_sample: True if library_key in obs and nunique > 1
         - Multi-sample + missing subject_key: append warning
         - Multi-sample + subject_key maps 1:1 to library_key: append distinctness warning
         - Single-sample + missing subject_key: append informational warning
         - Returns list of warning strings (empty = valid)
       - `check_confounding(obs: pd.DataFrame, batch_key: str, condition_key: str) -> bool`
         - Uses `pd.crosstab`, returns True if every batch maps to exactly one condition level
       - Integrate confounding into validate_subject_metadata: when condition_key and batch_key provided and present in obs, call check_confounding. Append warning if True.

    4. Export from `sc_tools/qc/__init__.py`: add `validate_subject_metadata`, `check_confounding` to imports and `__all__`.
  </action>
  <verify>
    <automated>python -m pytest sc_tools/tests/test_subject_metadata.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def validate_subject_metadata" sc_tools/qc/metadata.py` returns 1
    - `grep -c "def check_confounding" sc_tools/qc/metadata.py` returns 1
    - `grep "validate_subject_metadata" sc_tools/qc/__init__.py` finds the export
    - `grep -c "def test_" sc_tools/tests/test_subject_metadata.py` returns >= 7
    - All tests in test_subject_metadata.py pass
  </acceptance_criteria>
  <done>validate_subject_metadata returns correct warnings for all cases (multi-sample missing, 1:1 mapping, single-sample, confounded). check_confounding detects perfect confounding. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Panel-aware cell typing dispatch + tests (SCI-04)</name>
  <files>sc_tools/tl/celltype/annotate.py, sc_tools/tests/test_panel_dispatch.py</files>
  <read_first>
    - sc_tools/tl/celltype/annotate.py
    - sc_tools/tl/celltype/_base.py
    - sc_tools/tl/celltype/__init__.py
    - sc_tools/errors.py
    - .planning/phases/06-scientific-gaps/06-RESEARCH.md (Pattern 2: Panel Guard)
  </read_first>
  <behavior>
    - Test: adata with n_vars=40 + method="celltypist" raises SCToolsDataError
    - Test: adata with n_vars=40 + method="sctype" proceeds without error
    - Test: adata with n_vars=40 + method="custom_gates" proceeds without error
    - Test: adata with n_vars=40 + method="celltypist" + force_method=True logs warning but does not raise
    - Test: adata with n_vars=18000 + method="celltypist" proceeds without error (no panel restriction)
    - Test: panel_info dict is populated correctly (panel_detected, n_vars, restricted_methods)
  </behavior>
  <action>
    1. Create `sc_tools/tests/test_panel_dispatch.py` with tests using `adata_panel` fixture (from Task 1) and a regular n_vars=200 fixture.
       - For tests that would call the full backend (which needs real models), mock `get_backend` to return a mock that returns dummy results.
       - Test the guard logic directly by checking that SCToolsDataError is raised or not.

    2. Modify `sc_tools/tl/celltype/annotate.py` (per D-11, D-12, D-13):
       - Add at module level:
         ```python
         PANEL_VALIDATED_METHODS = {"sctype", "custom_gates"}
         WHOLE_TRANSCRIPTOME_METHODS = {"celltypist", "scgpt", "geneformer", "scarches", "singler"}
         PANEL_THRESHOLD = 1000
         ```
       - Add `force_method: bool = False` parameter to `annotate_celltypes()`.
       - Before `get_backend(method)` call, add panel guard:
         - `panel_detected = adata.n_vars < PANEL_THRESHOLD`
         - Check `adata.raw.n_vars` if available (per Pitfall 3: use full gene count, not HVG-filtered).
         - If panel_detected and method in WHOLE_TRANSCRIPTOME_METHODS:
           - If force_method: log warning "Panel detected (n_vars=X) but force_method used for 'Y'. Results may be unreliable."
           - Else: raise SCToolsDataError with suggestion "Use --method sctype or --force-method"
       - Store panel info in adata.uns: `adata.uns['panel_dispatch'] = {"panel_detected": panel_detected, "n_vars": adata.n_vars, "restricted_methods": sorted(PANEL_VALIDATED_METHODS) if panel_detected else []}` (per D-13)
  </action>
  <verify>
    <automated>python -m pytest sc_tools/tests/test_panel_dispatch.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PANEL_VALIDATED_METHODS" sc_tools/tl/celltype/annotate.py` finds the constant
    - `grep "WHOLE_TRANSCRIPTOME_METHODS" sc_tools/tl/celltype/annotate.py` finds the constant
    - `grep "force_method" sc_tools/tl/celltype/annotate.py` finds the parameter
    - `grep "panel_dispatch" sc_tools/tl/celltype/annotate.py` finds the uns storage
    - `grep -c "def test_" sc_tools/tests/test_panel_dispatch.py` returns >= 6
    - All tests in test_panel_dispatch.py pass
  </acceptance_criteria>
  <done>annotate_celltypes blocks whole-transcriptome methods for panels (n_vars < 1000), allows force_method override with warning, stores panel_dispatch info in uns. All tests green.</done>
</task>

</tasks>

<verification>
Run all phase 06 tests created so far:
```bash
python -m pytest sc_tools/tests/test_subject_metadata.py sc_tools/tests/test_panel_dispatch.py -v --tb=short
```
Existing tests still pass:
```bash
python -m pytest sc_tools/tests/test_qc.py -x -q
```
</verification>

<success_criteria>
- validate_subject_metadata handles all 4 cases (multi-sample missing, 1:1, single-sample, confounded) with correct warning strings
- check_confounding detects perfect confounding via crosstab
- annotate_celltypes rejects whole-transcriptome methods for panel data
- force_method override works with logged warning
- Panel info stored in adata.uns['panel_dispatch']
- All new tests pass, existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/06-scientific-gaps/06-01-SUMMARY.md`
</output>
