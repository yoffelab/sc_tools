---
phase: 09-sample-concatenation-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - sc_tools/qc/report_utils.py
  - sc_tools/assets/base_report_template.html
  - sc_tools/tests/test_qc.py
  - .claude/skills/k-dense-frontend/SKILL.md
autonomous: true
requirements: [MAINT-01, MAINT-02]

must_haves:
  truths:
    - "plotly is importable from a base sc_tools install without [pipeline] extras"
    - "HTML reports load plotly.js 3.4.0 matching the installed plotly.py 6.6.0"
    - "Test suite passes with updated CDN assertion"
  artifacts:
    - path: "pyproject.toml"
      provides: "plotly in base dependencies"
      contains: "plotly>=5.18"
    - path: "sc_tools/qc/report_utils.py"
      provides: "Updated CDN pin"
      contains: "plotly-3.4.0.min.js"
    - path: "sc_tools/assets/base_report_template.html"
      provides: "Updated CDN pin in template"
      contains: "plotly-3.4.0.min.js"
    - path: "sc_tools/tests/test_qc.py"
      provides: "Updated test assertion"
      contains: "plotly-3.4.0.min.js"
  key_links:
    - from: "sc_tools/qc/report_utils.py"
      to: "cdn.plot.ly/plotly-3.4.0.min.js"
      via: "CDN script tag"
      pattern: "plotly-3\\.4\\.0\\.min\\.js"
    - from: "sc_tools/assets/base_report_template.html"
      to: "cdn.plot.ly/plotly-3.4.0.min.js"
      via: "CDN script tag"
      pattern: "plotly-3\\.4\\.0\\.min\\.js"
---

<objective>
Promote plotly from optional [pipeline] dependency to base dependencies and update the stale CDN pin from plotly-2.27.0 to plotly-3.4.0 across all locations.

Purpose: Reports must render correctly with plotly.js 3.x (matching plotly.py 6.6.0) and plotly must be available without installing pipeline extras.
Output: Updated pyproject.toml, report_utils.py, base_report_template.html, test assertion, frontend skill doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sample-concatenation-maintenance/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote plotly to base dependencies (MAINT-01)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current dependency structure — base deps at line 36, [pipeline] extras around line 105)
  </read_first>
  <action>
    1. Add `"plotly>=5.18",` to the `[project.dependencies]` list (line 36-54 block), inserting after `"statannotations>=0.6",` (alphabetical order near bottom).
    2. Remove `"plotly>=5.18",` from the `[project.optional-dependencies]` `pipeline` list (around line 107).
    3. Remove `"plotly>=5.18",` from the `benchmark` list (around line 147) — it will be inherited from base deps.
    4. Remove `"plotly>=5.18",` from the `spatial` list (around line 166) — same reason.
    5. Keep plotly in any `dev` or `test` extras if present (unlikely, but check).

    Per MAINT-01: plotly promoted from optional [pipeline] dependency to base dependencies.
    Research confirms plotly 6.6.0 works with Python 3.10+, no compatibility issues.
  </action>
  <verify>
    <automated>cd /Users/junbumkim/Documents/sc_tools && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=[x for x in d['project']['dependencies'] if 'plotly' in x]; print('BASE:', deps); opt=d['project'].get('optional-dependencies',{}); pipe=[x for x in opt.get('pipeline',[]) if 'plotly' in x]; bench=[x for x in opt.get('benchmark',[]) if 'plotly' in x]; spat=[x for x in opt.get('spatial',[]) if 'plotly' in x]; print('PIPELINE:', pipe, 'BENCH:', bench, 'SPATIAL:', spat); assert len(deps)==1; assert len(pipe)==0; assert len(bench)==0; assert len(spat)==0; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml `[project.dependencies]` contains `"plotly>=5.18"`
    - pyproject.toml `[project.optional-dependencies]` pipeline list does NOT contain plotly
    - pyproject.toml `[project.optional-dependencies]` benchmark list does NOT contain plotly
    - pyproject.toml `[project.optional-dependencies]` spatial list does NOT contain plotly
  </acceptance_criteria>
  <done>plotly>=5.18 appears in base dependencies only, removed from all optional extras</done>
</task>

<task type="auto">
  <name>Task 2: Update Plotly CDN pin from 2.27.0 to 3.4.0 (MAINT-02)</name>
  <files>sc_tools/qc/report_utils.py, sc_tools/assets/base_report_template.html, sc_tools/tests/test_qc.py, .claude/skills/k-dense-frontend/SKILL.md</files>
  <read_first>
    - sc_tools/qc/report_utils.py (line 517 — current CDN string)
    - sc_tools/assets/base_report_template.html (line 8 — current CDN tag)
    - sc_tools/tests/test_qc.py (line 1311 — current assertion)
    - .claude/skills/k-dense-frontend/SKILL.md (lines 29 and 169 — current version references)
  </read_first>
  <action>
    Replace all instances of `plotly-2.27.0.min.js` with `plotly-3.4.0.min.js` in these 4 files:

    1. **sc_tools/qc/report_utils.py** line 517: Change
       `plotly_cdn = '<script src="https://cdn.plot.ly/plotly-2.27.0.min.js"></script>'`
       to
       `plotly_cdn = '<script src="https://cdn.plot.ly/plotly-3.4.0.min.js"></script>'`

    2. **sc_tools/assets/base_report_template.html** line 8: Change
       `<script src="https://cdn.plot.ly/plotly-2.27.0.min.js"></script>`
       to
       `<script src="https://cdn.plot.ly/plotly-3.4.0.min.js"></script>`

    3. **sc_tools/tests/test_qc.py** line 1311: Change
       `assert "plotly-2.27.0.min.js" in content`
       to
       `assert "plotly-3.4.0.min.js" in content`

    4. **.claude/skills/k-dense-frontend/SKILL.md** line 29 and 169: Change
       `plotly-2.27.0.min.js` references to `plotly-3.4.0.min.js`
       (table row on line 29: `| Charts | Plotly 3.4.0 | https://cdn.plot.ly/plotly-3.4.0.min.js |`)

    Per MAINT-02 + research override: Do NOT use `plotly-latest.min.js` — it is frozen at v1.58.5 on the CDN.
    Use `plotly-3.4.0.min.js` which matches the installed plotly.py 6.6.0 bundle.
  </action>
  <verify>
    <automated>cd /Users/junbumkim/Documents/sc_tools && grep -c "plotly-3.4.0.min.js" sc_tools/qc/report_utils.py sc_tools/assets/base_report_template.html sc_tools/tests/test_qc.py .claude/skills/k-dense-frontend/SKILL.md && grep -c "plotly-2.27.0.min.js" sc_tools/qc/report_utils.py sc_tools/assets/base_report_template.html sc_tools/tests/test_qc.py .claude/skills/k-dense-frontend/SKILL.md && pytest sc_tools/tests/test_qc.py::TestBaseTemplate -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "plotly-3.4.0.min.js" sc_tools/qc/report_utils.py` returns a match
    - `grep "plotly-3.4.0.min.js" sc_tools/assets/base_report_template.html` returns a match
    - `grep "plotly-3.4.0.min.js" sc_tools/tests/test_qc.py` returns a match
    - `grep "plotly-3.4.0.min.js" .claude/skills/k-dense-frontend/SKILL.md` returns a match
    - `grep "plotly-2.27.0.min.js" sc_tools/qc/report_utils.py` returns NO match
    - `grep "plotly-2.27.0.min.js" sc_tools/assets/base_report_template.html` returns NO match
    - `pytest sc_tools/tests/test_qc.py::TestBaseTemplate -x -q` passes
  </acceptance_criteria>
  <done>All 4 files updated from plotly-2.27.0 to plotly-3.4.0, test assertion updated and passing</done>
</task>

</tasks>

<verification>
- `python -c "import plotly"` succeeds without [pipeline] extras
- `grep -r "plotly-2.27.0" sc_tools/` returns zero matches
- `grep -r "plotly-3.4.0" sc_tools/` returns matches in report_utils.py, base_report_template.html, and test_qc.py
- `pytest sc_tools/tests/test_qc.py::TestBaseTemplate -x -q` passes
- `make lint` passes
</verification>

<success_criteria>
- plotly importable from base install
- All CDN pins updated to 3.4.0
- Test suite green
- Lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-sample-concatenation-maintenance/09-01-SUMMARY.md`
</output>
