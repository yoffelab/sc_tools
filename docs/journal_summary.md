# Journal Summary: sc_tools Repository

Condensed summary of root `Journal.md` for context. Full entries and rationale are in `Journal.md`.

## Repo scope

- **sc_tools** is a reusable spatial omics toolkit and pipeline. Project-specific work lives under `projects/<data_type>/<project_name>/`. Root Journal covers repo structure, phasing, checkpoint names, and script roles only.

## Recent phase (2026-03)

- **Automated cell typing + post-celltyping QC report (2026-03-06):** (Plan A) `sc_tools/tl/celltype/` subpackage. `annotate_celltypes(adata, method=...)` dispatcher with Protocol-based `CelltypeBackend` registry (same pattern as deconvolution). Backends: ScType (pure numpy, cluster or per-cell assignment, min_score threshold), custom hierarchical gating (DFS, zero deps), CellTypist (soft dep), ensemble (majority_vote, weighted_vote). Stubs for scArches, Geneformer, scGPT, SingleR. `apply_celltype_map()` absorbs project-local `apply_leiden_mapping()` from robin — writes `obs[celltype]`, `obs[celltype_broad]`, `uns[celltype_colors]` from JSON map. Bundled marker DBs and gating templates. `[celltyping]` and `[foundation]` extras in pyproject.toml. 37 new tests. (Plan B) `generate_post_celltyping_report()` full overhaul: new purple-header template, celltype composition section, Phase 3 vs Phase 4 bio benchmark table (bio column primary), fixed `ranking_rows` bug (was never built), `comparison_df_p3` + `marker_genes` params. `qc_celltype_abundance()` stacked bar chart added to `sc_tools.pl`. Tabbed navigation (Option B inline embed): reports 2+3+4 each embed prior-phase reports as tabs; `_find_latest_report`, `_extract_body_content`, `_extract_head_css`, `_wrap_with_tabs` in `report_utils.py`. `qc_post_celltyping` Snakemake rule added to robin + ggo_visium. 17 new tests. 574 total pass, lint clean.
- **Option A: dual phase-name support (2026-03-06):** `sc_tools/validate.py` now accepts both new slugs (`qc_filter`, `metadata_attach`, `preprocess`, `scoring`, `celltype_manual`) and old p-codes (`p1`/`p2`/`p3`/`p35`/`p4`). Old codes emit `DeprecationWarning` naming the new slug and referencing Architecture.md. `validate_p1` through `validate_p4` functions kept for backward compat, marked deprecated in docstrings. `scripts/validate_checkpoint.py` CLI `--phase` accepts all 10 values; epilog shows slug-to-old-code table. `projects/create_project.sh` Snakefile template updated: slug-based rule names, new checkpoint filenames (`adata.raw.h5ad` not `adata.raw.p1.h5ad`), slug `--phase` flags, comment block marking old p-codes as deprecated. Mission.md and CLAUDE.md templates updated. 22 new tests; 522 total pass, lint clean.
- **Registry schema extension + Phase DAG (2026-03-06):** Three extensions. (1) `sc_tools/pipeline.py`: `PhaseSpec` + `STANDARD_PHASES` dict with 10 semantic slugs replacing p0a/p0b/p1/p2/p3/p35/p4. Slugs: `ingest_raw`, `ingest_load`, `qc_filter`, `metadata_attach`, `preprocess`, `demographics`, `scoring`, `celltype_manual`, `biology`, `meta_analysis`. `extend_dag()` for custom phases. `get_available_next()` respects `iterative=True` (celltype_manual). `get_phase_checkpoint(slug)` returns expected path. (2) Registry schema: `domain`/`imaging_modality` on projects; `file_role`/`validated`/`n_obs`/`n_vars` on datasets; new `project_phases` table (composite PK `(project_id, phase)`, upserted). Methods: `upsert_phase`, `get_phase`, `list_phases`, `mark_dataset_validated`. (3) Alembic migrations `0001` (baseline) + `0002` (new columns + table). MCP server: `get_phase_status`, `set_phase_status` added. README Mermaid fixed (TD, no nested subgraphs). 59 new tests (500 total), lint clean. Phase slugs stored as strings in DB; DAG lives in Python code only.
- **Storage + Registry + MCP (2026-03-06):** Three new interlocking layers. (1) `sc_tools/storage.py`: fsspec URI abstraction (`resolve_fs`, `open_file`, `with_local_copy`, `smart_read_h5ad`, `smart_write_checkpoint`, `smart_read_csv`). Supports local, SFTP/HPC, S3, GCS, Azure, Box. Extras: `[storage]`, `[storage-box]`. (2) `sc_tools/registry.py`: SQLAlchemy ORM with 4 tables (projects, datasets, slurm_jobs, agent_tasks). SQLite default, Postgres via env var. Extras: `[registry]`. CLI: `python -m sc_tools registry status`. Alembic migrations in `sc_tools/migrations/`. (3) `sc_tools/mcp/`: two FastMCP servers (`tools_server.py` with 8 tools, `registry_server.py` with 7 tools). `.mcp.json` at repo root for Claude Code auto-discovery. Ingest updates: `load_imc_sample()` accepts remote h5ad URIs; `load_batch_manifest()` / `collect_all_batches()` support remote manifests. 441 tests pass (47 new), lint clean.
- **IMC segmentation benchmark pipeline (2026-03-06):** Complete overhaul of `sc_tools.bm` benchmark framework. 21 new files, 8 modified. New `sc_tools/data/imc/benchmark/` subpackage with catalog (8 internal + 5 public datasets), config (YAML I/O), prepare (normalization, channel extraction, prob map generation), public dataset download/standardization. Four segmentation strategies: (1) Ilastik prob map + DL, (2) DNA-only + prob map + DL, (3) HuggingFace pretrained (CellViT/SAM/HoVer-Net), (4) SegFormer trainable N-channel. Runner with resume, SLURM job generation, CLI (`python -m sc_tools.bm.cli`). Extended metrics: panoptic quality, boundary F1, cell type preservation. Cross-dataset analysis with Wilcoxon + BH correction. 7 new plot functions, extended HTML report template. `benchmark-extended` dep group in pyproject.toml. 66 new tests (394 total pass). Lint clean.
- **Integration benchmark + 4th QC report (2026-03-06):** Ran 9-method integration benchmark on ggo_human IMC (~248K cells, 38 markers). Results: Z-score+Harmony (0.634) > IMC Pheno+Harmony (0.633) > IMC Phenotyping (0.627) > scANVI (0.621) > ComBat (0.568) > PCA (0.564) > scVI (0.555) > Harmony (0.553) > CytoVI (0.486). Fixed Harmony PyTorch bug (direct harmonypy call with tensor detection). Added `comparison_df` pass-through to skip report recomputation. **Key design decision:** 4-report QC workflow: pre-filter → post-filter → post-integration (batch-focused) → post-celltyping (full bio metrics). Batch score is primary for integration selection; bio metrics meaningful only after validated celltyping. Integration benchmark stores intermediates in `results/tmp/integration_test/{method}.h5ad`; records selected method in `results/integration_method.txt`. Architecture.md updated (Sections 2.3, 2.5, Phase 3, Phase 4). Mission.md updated.
- **PyPI deployment (2026-03-04):** CI/CD step 4 complete. Created `LICENSE` (MIT), `.github/workflows/publish.yml` (trusted publishing via OIDC), updated `pyproject.toml` (`requires-python >= 3.10`, SPDX license string, package-data for bundled JSON/HTML). `python -m build` produces clean wheel/sdist with all data files. One-time PyPI trusted publisher setup still needed before first release. Next: GitHub Actions tests.yml (step 5).
- **IMC image loading (2026-03-04):** Added `IMCPanelMapper`, `build_imc_composite()`, `load_imc_sample(load_images=True)`, `load_he_image()`, `plot_imc_composite()`, `plot_imc_channel()`. Key finding from data exploration: IMC TIFF structure is a single multi-channel stack `{roi_id}_full.tiff` (C, H, W), NOT individual per-channel files. Companion CSV `{roi_id}_full.csv` maps index → `MarkerName(IsotopeTag)` (e.g. `CD3(Er170)`). Also `_full_mask.tiff`, `_full_nucmask.tiff`, `_Probabilities.tiff`. Two panel CSV formats: per-ROI `*_full.csv` (index+name) and project-level `channel_labels.csv` (channel, Target, Metal_Tag, Atom, full, ilastik). `IMCPanelMapper` resolves protein name/isotope tag/partial name → stack index; reads both. RGB composite + full arcsinh-normalized stack stored in `uns['spatial'][sample_id]` (squidpy-compatible; `sc.pl.spatial` works). Architecture.md Section 2.2b added with full IMC image schema. 291 tests pass, lint clean.
- **Documentation overhaul + priority reorder (2026-03-04):** README.md rewritten as toolkit-level doc (removed GGO-specific content); Phase 0 added to Mermaid diagram and phase table; API module table corrected (`sc_tools.pl/tl/qc/pp/ingest/validate/memory`); full extras table added. `project_setup.md` updated with all 10 optional-dependency groups. `projects/README.md` refreshed (phase table, paths). `containers/README.md` deleted (duplicate of project_setup.md). IMC loader API fixed: `load_imc_sample()` now accepts `processed_dir` (not `h5ad_path`); auto-discovers `cells.h5ad` inside. CosMx `REQUIRED_COLUMNS` updated to `{"sample_id", "cosmx_dir"}`. **Priority reorder:** (1) IMC ingestion pipeline end-to-end, (2) PyPI deployment, (3) Phase 0b checkpoint script. CosMx loader deprioritized; documented three panel tiers (1k, 6k, full_library) with different scale characteristics.
- **Phase 0 AnnData/SpatialData + CosMx loader (2026-03-04):** Clarified Phase 0 as two sub-steps: (0a) platform tools → `data/{sample_id}/outs/`; (0b) per-sample load → `data/{sample_id}/adata.p0.h5ad` (all modalities) or `data/{sample_id}/spatialdata.zarr` (Visium HD / Xenium, optional). Phase 1 now defined as QC + concat from Phase 0b checkpoints. Added `load_cosmx_sample()` as a pending TODO for CosMx flat file / RDS ingestion. Architecture.md Section 2.1 updated with new checkpoints; Section 2.2 adds adata.p0.h5ad metadata requirements. CLAUDE.md phase table split 0a/0b. skills.md §1 adds modality loader table.
- **Publication figure guidelines + skills.md reorg (2026-03-04):** Added §12 with journal-specific figure standards (Nature/Cell/Science dimensions, DPI, fonts, Okabe-Ito/Paul Tol palettes, statistical reporting, marsilea for composites, pre-submission checklist). Reorganized skills.md into 4 parts: (I) Analysis Pipeline §1-6, (II) Spatial §7-9, (III) Stats and Visualization §10-12, (IV) Engineering §13-17. Eliminated §11.5; consolidated colors into §11. CI/CD roadmap in Mission.md converted to clean table format.
- **visium_hd_cell modality + robin batch manifests (2026-03-04):** Added `visium_hd_cell` modality for SpaceRanger 4 cell segmentation output. New `load_visium_hd_cell_sample()` loader, cell-level QC thresholds, routes to xenium recipe (single-cell resolution). Updated `create_project.sh`. Created Phase 0 batch manifests for robin (batch1: 8 samples, batch2: 8 samples, all_samples.tsv: 16 total). Updated robin config.yaml with SR4 paths. 248 tests pass, lint clean.
- **Phase 0 ingestion + checkpoint validation (2026-03-04):** Two new modules. (1) `sc_tools.validate`: checkpoint validation for p1-p4 enforcing Architecture.md Section 2.2 contracts; auto-fix batch->raw_data_dir; CLI wrapper `scripts/validate_checkpoint.py` for Snakemake. (2) `sc_tools.ingest`: batch manifest system (per-batch TSVs in metadata/phase0/, collect to all_samples.tsv), Space Ranger/Xenium/IMC command builders, modality-specific AnnData loaders (Visium/HD/Xenium/IMC), concat_samples(). Updated robin and ggo_visium Snakefiles with validation sentinels and Phase 0 rules. create_project.sh template updated. 56 tests pass, lint clean.
- **QC HTML report layout overhaul (2026-03-04):** Rewrote QC report from flat PNG grid to structured 6-row layout with inline plot generation. Rows: (1) 2x2 pre/post histograms, (2) violin pre/post, (3) %MT per sample, (4) bar chart linear+log10, (5) violin grouped linear+log10, (6) scatter matrix. Added `log_scale` param to `qc_sample_comparison_bar` and `qc_sample_violin_grouped`; new `qc_pct_mt_per_sample` function. `generate_qc_report()` now accepts `adata_post` and generates plots inline (no file globbing). Caption font bumped to 1.0rem/#555. Verified on ggo_visium and robin_round2.
- **Sphinx documentation site (2026-03-03):** CI/CD step 3 complete. `docs/` with pydata-sphinx-theme + myst-nb + sphinx-design. Six API pages (pp, pl, tl, qc, memory, utils) using `.. autofunction::` inline (no autosummary stub generation — hangs on heavy deps). Six tutorial notebooks (synthetic data, static outputs, `nb_execution_mode="off"`). `.readthedocs.yaml` for RTD, `.github/workflows/docs.yml` for CI. `autodoc_mock_imports = []` (empty): mocking cupy/torch caused anndata 0.11.4 singledispatch/find_spec failures; all soft deps use try/except guards so no mocks needed. Fixed `sc_tools/memory/profiling.py` anndata import to `TYPE_CHECKING` guard. `build succeeded` with zero warnings. `make docs` target added. Next CI/CD step: PyPI deployment (4).

## Recent phase (2026-02)

- **Phase 3 preprocessing module (2026-02-28):** `sc_tools.pp` -- modality-aware preprocessing with GPU auto-detection (rapids-singlecell/scanpy). `normalize.py`: `backup_raw`, `normalize_total`, `log_transform`, `scale`, `arcsinh_transform` (IMC), `filter_genes_by_pattern` (MT/RP/HB). `integrate.py`: `run_scvi`, `run_harmony`, `run_cytovi` (all soft deps). `reduce.py`: `pca`, `neighbors` (auto use_rep: X_scVI > X_cytovi > X_pca_harmony > X_pca), `umap`, `leiden`, `cluster`, `run_utag` (spatial clustering, soft dep). `recipes.py`: `preprocess(modality=visium|visium_hd|xenium|cosmx|imc)` dispatches to modality-specific pipelines. Visium: scVI on raw counts (seurat_v3 HVG) or normalize+log1p+seurat HVG for non-VAE. IMC: arcsinh(X/5) not log1p. 38 unit tests, 130 total pass, lint clean. `skills.md` updated with modality tables and GPU notes. `pyproject.toml`: added `[integration]` (harmonypy), `[spatial]` (utag), expanded `[gpu]` (rapids-singlecell).
- **QC report module overhaul (2026-02-28):** `sc_tools/qc/sample_qc.py`: `filter_spots()` (modality-aware defaults), `compute_sample_metrics()` (per-sample aggregates), `classify_samples()` (absolute + adaptive MAD outlier detection), `apply_qc_filter()` (backup + filter + remove). 3 new cross-sample plots (`qc_sample_comparison_bar`, `qc_sample_violin_grouped`, `qc_sample_scatter_matrix`). HTML report via `sc_tools/qc/report.py` + Jinja2 template. `scripts/run_qc_report.py` expanded with `--modality`, `--apply-filter`, `--mad-multiplier`. 28 tests pass, lint clean.
- **Gene set scoring redesign (2026-02-28):** Full overhaul of Phase 3.5b gene set infrastructure. (1) `sc_tools/data/hallmark_human.json`: 50 bundled MSigDB Hallmark sets, stripped from robin metadata, offline. (2) `sc_tools/tl/gene_sets.py`: `load_hallmark`, `load_msigdb_json`, `load_gmt`, `list_gene_sets`, `validate_gene_signatures`, `merge_gene_signatures`, `update_gene_symbols`, `save_gene_signatures`. (3) `score_signature(method=...)`: `"scanpy"` (default), `"ucell"` (pyucell soft dep, rank AUC), `"ssgsea"` (gseapy soft dep); records `uns["scoring_method"]`. (4) `sc_tools/tl/gsea.py`: `run_ora` (Fisher exact + BH), `run_gsea_pseudobulk` (prerank via gseapy). (5) `sc_tools/pl/gsea.py`: `plot_gsea_dotplot`. Optional deps: `pip install sc-tools[geneset]`. All 80 tests pass; lint clean.
- **Generic deconvolution module (2026-02-26/27):** `sc_tools.tl.deconvolution()` with backend registry (cell2location, tangram, destvi). Per-library backed loading with memory profiling. Output: `obsm['cell_type_proportions']`, `obs['{method}_argmax']`. Fixed NaN bug for Seurat SCTransform references (negative values triggered double normalisation). Method-specific output naming: `adata.deconvolution.{method}.h5ad`. ggo_visium: both Tangram and Cell2location completed (29952 spots x 31 types). robin: Tangram done (280K spots x 39 types), Cell2location running on CPU.
- **Deconvolution spatial plots (2026-02-27):** `scripts/plot_deconvolution_spatial.py` saves per-library PNGs at 300 DPI alongside PDF. Method-specific output dirs (`figures/deconvolution/{method}/`). Spot sizing `120000/n_spots`; 98th percentile vmax for contrast; white background. Both Snakefiles updated (Phase 3.5b rules).
- **sc_tools skills as Cursor/Claude Code skill:** Repository `skills.md` exposed as `.cursor/skills/sc-tools-skills/SKILL.md` and `.claude/skills/sc-tools-skills/SKILL.md`; agent follows it for analysis and coding. Sandbox/local defaults: **Apptainer + Snakemake** (project_setup.md; skills.md §11). Docker used only for building the SIF image.
- **Container setup (current):** Docker builds image → `apptainer build containers/sc_tools.sif docker-daemon://sc_tools:latest`. All pipeline runs use `run_apptainer.sh` or Snakemake with `--use-apptainer`. On macOS, Apptainer requires a Linux VM or HPC; Docker is the local fallback.
- **Claude Code config added (2026-02-26):** CLAUDE.md at repo root; `.claude/skills/journal-and-mission/` and `.claude/skills/sc-tools-skills/` ported from Cursor. `.claude/settings.json` allowlists make, ruff, pytest, snakemake, git, apptainer.
- **Workflow:** journal_summary.md added everywhere; Mission.md is the todo list; in work mode the agent updates Mission after each prompt. Skill and Cursor rule enforce this; create_project.sh creates journal_summary.md for new projects.
- **CI/CD:** Linting (1) done. Snakemake (2) already adopted (each project has Snakefile). Order: 3 Sphinx, 4 PyPI, 5 GitHub Actions (last; testing complex). Containers: Apptainer (Linux/HPC, primary); Docker (macOS/Windows, fallback); auto-configure via `run_container.sh`.

- **Checkpoint nomenclature:** Standard filenames are in Architecture.md Section 2.1 (`adata.raw.h5ad`, `adata.normalized.h5ad`, `adata.scored.h5ad`, etc.). Legacy p-code names (`adata.raw.p1.h5ad`, etc.) accepted during transition but deprecated. ggo_visium checkpoint files were renamed and Snakefile updated to match.
- **Phasing:** Pipeline is 7 phases (1–7) with Phase 3.5 (Demographics) and 3.5b (Gene scoring, automated cell typing, deconvolution) branching from Phase 3. Phase 4 (Manual Cell Typing) is skippable if 3.5b is adequate. Gene signature storage: p35 uses `obsm['signature_score']` and `obsm['signature_score_z']` (full-path columns); `get_signature_df`/`get_signature_columns` read from obsm first, fall back to obs.
- **Layout:** All metadata, results, figures are project-specific. No root-level metadata/results/figures. Legacy scripts in `scripts/old_code/` per project. Single toolkit package: `sc_tools` only (ggo_tools/ggo_analysis removed).

## Key conventions

- Mission/Journal split: root = toolkit and pipeline; each project has its own Mission.md and Journal.md.
- New project: `./projects/create_project.sh <project_name> <data_type>`.
- Testing: two layers — package tests (`sc_tools/tests/`), project tests (`projects/.../tests/`). Order: ggo_visium tests first, then sc_tools tests, then implement.
