# REFERENCE ONLY — pyproject.toml is the source of truth.
# Install via: pip install -e ".[deconvolution,registry,mcp]"
# Requires Python >= 3.11
#
# This file is kept for quick reference of core dependencies.

# Core scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Visualization and statistical annotation
matplotlib>=3.7.0
seaborn>=0.12.0
statannotations>=0.6.0
pingouin>=0.5.0

# Single-cell / spatial analysis
scanpy>=1.9.0
squidpy>=1.3.0
anndata>=0.10.0

# Deconvolution (optional heavy deps; install separately or use [deconvolution] extra)
# scvi-tools>=1.0.0
# tangram-sc>=1.0.0

# Gene set scoring and enrichment testing (optional; use [geneset] extra)
# gseapy>=1.0
# pyucell>=0.1

# Transcription factor / pathway activity (optional; use [decoupler] extra)
# decoupler>=1.4

# Utilities
tqdm>=4.65.0
dask>=2023.0.0
Pillow>=10.0.0
tifffile>=2023.0.0
